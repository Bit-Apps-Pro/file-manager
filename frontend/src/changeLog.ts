const changeLogs = {
  improvements: [],
  fixed: [
    'Updated regex to allow plain HTML uploads while blocking files with embedded JavaScript to prevent XSS across all file types.'
  ]
}

export default changeLogs
