const changeLogs = {
  improvements: [
    'The user permission grant UI has been updated, allowing users to search and grant permissions more efficiently. This resolves out-of-memory issues on the permission page.',
    'Previously, dashboard requests were handled via AJAX, but they are now managed through the REST API.',
    'Theme updated.',
    'Log deletion option added',
    'PHP packages updated.',
    'Users can now try certain plugins through the changelog modal.'
  ],
  fixed: ['Search functionality in the shortcode view.']
}

export default changeLogs
