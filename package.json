{
  "name": "file-manager",
  "version": "5.0.2",
  "description": "Manage your file the way you like. You can upload, delete, copy, move, rename, compress, extract files. You don't need to worry about ftp. It is realy simple and easy to use.",
  "author": "Aftabul Islam",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "sass:compile": "node-sass sass/style.sass -o css --output-style expanded --source-map true",
    "sass:minify": "cleancss --format breakWith=lf --source-map --source-map-inline-sources --output css/style.min.css css/style.css",
    "sass:build": "npm run sass:compile && npm run sass:minify",
    "sass:watch": "onchange 'sass/**/*.sass' -- npm run sass:build"
  },
  "dependencies": {
    "clean-css-cli": "^5.5.2",
    "concurrently": "^7.0.0",
    "node-sass": "^7.0.1",
    "onchange": "^7.1.0"
  }
}
